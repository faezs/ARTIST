// generated by RequestProject/Ccc.lean (HashemiCcc.lean) - do not edit.
// one thread per sample (jvp) or per box; x/dx/lo/hi/sc are (N, NIN) row-major, y/dy/olo/ohi/oL are (N, NOUT).
kernel void mk_footLong_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_footLong_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_footLong_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_footLong_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_azRate_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_azRate_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_azRate_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_azRate_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_beamAxis_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[3], tdy[3];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_beamAxis_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 3; ++k) { y[i*3+k] = ty[k]; dy[i*3+k] = tdy[k]; }
}
kernel void mk_beamAxis_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[3], rhi[3], rL[3];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_beamAxis_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 3; ++k) { olo[i*3+k] = rlo[k]; ohi[i*3+k] = rhi[k]; oL[i*3+k] = rL[k]; }
}
kernel void mk_bisectStep_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[7], tdx[7], ty[2], tdy[2];
  for (int k = 0; k < 7; ++k) { tx[k] = x[i*7+k]; tdx[k] = dx[i*7+k]; }
  hk_bisectStep_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tdx, ty, tdy);
  for (int k = 0; k < 2; ++k) { y[i*2+k] = ty[k]; dy[i*2+k] = tdy[k]; }
}
kernel void mk_bisectStep_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[7], thi[7], tsc[7], rlo[2], rhi[2], rL[2];
  for (int k = 0; k < 7; ++k) { tlo[k] = lo[i*7+k]; thi[k] = hi[i*7+k]; tsc[k] = sc[i*7+k]; }
  hk_bisectStep_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 2; ++k) { olo[i*2+k] = rlo[k]; ohi[i*2+k] = rhi[k]; oL[i*2+k] = rL[k]; }
}
kernel void mk_boltStress_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_boltStress_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_boltStress_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_boltStress_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_braceHeight_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_braceHeight_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_braceHeight_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_braceHeight_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_cableDrop_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_cableDrop_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_cableDrop_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_cableDrop_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_capSag_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_capSag_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_capSag_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_capSag_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_captureS_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_captureS_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_captureS_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_captureS_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_celsius_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_celsius_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_celsius_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_celsius_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_clearance_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[7], tdx[7], ty[1], tdy[1];
  for (int k = 0; k < 7; ++k) { tx[k] = x[i*7+k]; tdx[k] = dx[i*7+k]; }
  hk_clearance_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_clearance_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[7], thi[7], tsc[7], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 7; ++k) { tlo[k] = lo[i*7+k]; thi[k] = hi[i*7+k]; tsc[k] = sc[i*7+k]; }
  hk_clearance_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_coilCapture_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_coilCapture_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_coilCapture_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_coilCapture_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_coilProfile_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[15], tdx[15], ty[8], tdy[8];
  for (int k = 0; k < 15; ++k) { tx[k] = x[i*15+k]; tdx[k] = dx[i*15+k]; }
  hk_coilProfile_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tdx, ty, tdy);
  for (int k = 0; k < 8; ++k) { y[i*8+k] = ty[k]; dy[i*8+k] = tdy[k]; }
}
kernel void mk_coilProfile_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[15], thi[15], tsc[15], rlo[8], rhi[8], rL[8];
  for (int k = 0; k < 15; ++k) { tlo[k] = lo[i*15+k]; thi[k] = hi[i*15+k]; tsc[k] = sc[i*15+k]; }
  hk_coilProfile_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 8; ++k) { olo[i*8+k] = rlo[k]; ohi[i*8+k] = rhi[k]; oL[i*8+k] = rL[k]; }
}
kernel void mk_conicHitS_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[8], tdx[8], ty[1], tdy[1];
  for (int k = 0; k < 8; ++k) { tx[k] = x[i*8+k]; tdx[k] = dx[i*8+k]; }
  hk_conicHitS_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_conicHitS_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[8], thi[8], tsc[8], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 8; ++k) { tlo[k] = lo[i*8+k]; thi[k] = hi[i*8+k]; tsc[k] = sc[i*8+k]; }
  hk_conicHitS_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_conicSlope_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_conicSlope_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_conicSlope_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_conicSlope_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_conicZ_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_conicZ_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_conicZ_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_conicZ_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_constraints_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[30], tdy[30];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_constraints_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 30; ++k) { y[i*30+k] = ty[k]; dy[i*30+k] = tdy[k]; }
}
kernel void mk_constraints_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[30], rhi[30], rL[30];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_constraints_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 30; ++k) { olo[i*30+k] = rlo[k]; ohi[i*30+k] = rhi[k]; oL[i*30+k] = rL[k]; }
}
kernel void mk_constraintsGrooved_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[42], tdy[42];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_constraintsGrooved_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 42; ++k) { y[i*42+k] = ty[k]; dy[i*42+k] = tdy[k]; }
}
kernel void mk_constraintsGrooved_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[42], rhi[42], rL[42];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_constraintsGrooved_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 42; ++k) { olo[i*42+k] = rlo[k]; ohi[i*42+k] = rhi[k]; oL[i*42+k] = rL[k]; }
}
kernel void mk_cross3_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[3], tdy[3];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_cross3_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 3; ++k) { y[i*3+k] = ty[k]; dy[i*3+k] = tdy[k]; }
}
kernel void mk_cross3_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[3], rhi[3], rL[3];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_cross3_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 3; ++k) { olo[i*3+k] = rlo[k]; ohi[i*3+k] = rhi[k]; oL[i*3+k] = rL[k]; }
}
kernel void mk_dPipe_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_dPipe_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_dPipe_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_dPipe_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_darcyF_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_darcyF_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_darcyF_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_darcyF_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_deadPoint_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_deadPoint_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_deadPoint_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_deadPoint_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_degCostRaw_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_degCostRaw_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_degCostRaw_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_degCostRaw_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_degradRate_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_degradRate_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_degradRate_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_degradRate_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_degradStep_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_degradStep_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_degradStep_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_degradStep_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_degraded_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_degraded_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_degraded_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_degraded_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_delayOf_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_delayOf_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_delayOf_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_delayOf_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_delivered_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_delivered_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_delivered_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_delivered_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_dishAxes_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[9], tdy[9];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_dishAxes_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 9; ++k) { y[i*9+k] = ty[k]; dy[i*9+k] = tdy[k]; }
}
kernel void mk_dishAxes_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[9], rhi[9], rL[9];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_dishAxes_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 9; ++k) { olo[i*9+k] = rlo[k]; ohi[i*9+k] = rhi[k]; oL[i*9+k] = rL[k]; }
}
kernel void mk_dishPower_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[24], tdx[24], ty[5], tdy[5];
  for (int k = 0; k < 24; ++k) { tx[k] = x[i*24+k]; tdx[k] = dx[i*24+k]; }
  hk_dishPower_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tx[15], tx[16], tx[17], tx[18], tx[19], tx[20], tx[21], tx[22], tx[23], tdx, ty, tdy);
  for (int k = 0; k < 5; ++k) { y[i*5+k] = ty[k]; dy[i*5+k] = tdy[k]; }
}
kernel void mk_dishPower_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[24], thi[24], tsc[24], rlo[5], rhi[5], rL[5];
  for (int k = 0; k < 24; ++k) { tlo[k] = lo[i*24+k]; thi[k] = hi[i*24+k]; tsc[k] = sc[i*24+k]; }
  hk_dishPower_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 5; ++k) { olo[i*5+k] = rlo[k]; ohi[i*5+k] = rhi[k]; oL[i*5+k] = rL[k]; }
}
kernel void mk_dishReflect_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[18], tdx[18], ty[7], tdy[7];
  for (int k = 0; k < 18; ++k) { tx[k] = x[i*18+k]; tdx[k] = dx[i*18+k]; }
  hk_dishReflect_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tx[15], tx[16], tx[17], tdx, ty, tdy);
  for (int k = 0; k < 7; ++k) { y[i*7+k] = ty[k]; dy[i*7+k] = tdy[k]; }
}
kernel void mk_dishReflect_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[18], thi[18], tsc[18], rlo[7], rhi[7], rL[7];
  for (int k = 0; k < 18; ++k) { tlo[k] = lo[i*18+k]; thi[k] = hi[i*18+k]; tsc[k] = sc[i*18+k]; }
  hk_dishReflect_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 7; ++k) { olo[i*7+k] = rlo[k]; ohi[i*7+k] = rhi[k]; oL[i*7+k] = rL[k]; }
}
kernel void mk_dot3_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[1], tdy[1];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_dot3_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_dot3_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_dot3_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_dpLam_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_dpLam_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_dpLam_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_dpLam_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_dpTurb_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_dpTurb_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_dpTurb_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_dpTurb_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_driveAz_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_driveAz_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_driveAz_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_driveAz_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_driveEl_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_driveEl_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_driveEl_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_driveEl_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_edgeClipAt_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[2], tdy[2];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_edgeClipAt_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 2; ++k) { y[i*2+k] = ty[k]; dy[i*2+k] = tdy[k]; }
}
kernel void mk_edgeClipAt_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[2], rhi[2], rL[2];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_edgeClipAt_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 2; ++k) { olo[i*2+k] = rlo[k]; ohi[i*2+k] = rhi[k]; oL[i*2+k] = rL[k]; }
}
kernel void mk_edgeDepth_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_edgeDepth_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_edgeDepth_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_edgeDepth_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_effCounter_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_effCounter_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_effCounter_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_effCounter_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_effNtu_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_effNtu_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_effNtu_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_effNtu_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_elPower_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_elPower_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_elPower_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_elPower_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_elRate_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_elRate_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_elRate_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_elRate_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_expansionFrac_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_expansionFrac_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_expansionFrac_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_expansionFrac_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_facetSpot_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_facetSpot_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_facetSpot_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_facetSpot_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_filmTemp_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_filmTemp_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_filmTemp_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_filmTemp_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_focusShift_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_focusShift_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_focusShift_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_focusShift_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_frictionBlasius_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_frictionBlasius_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_frictionBlasius_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_frictionBlasius_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_frictionLam_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_frictionLam_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_frictionLam_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_frictionLam_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_hCoil_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_hCoil_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_hCoil_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_hCoil_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_hWind_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_hWind_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_hWind_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_hWind_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_hangerLength_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_hangerLength_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_hangerLength_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_hangerLength_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_hashemiEnv_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device const float* hist_all [[buffer(2)]], device const float* ret_all [[buffer(3)]], device const float* dr_all [[buffer(4)]], device float* y [[buffer(5)]], device float* dy [[buffer(6)]], device const int* n [[buffer(7)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[47], tdx[47], ty[96], tdy[96];
  for (int k = 0; k < 47; ++k) { tx[k] = x[i*47+k]; tdx[k] = dx[i*47+k]; }
  hk_hashemiEnv_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tx[15], tx[16], tx[17], tx[18], tx[19], tx[20], tx[21], tx[22], tx[23], tx[24], tx[25], tx[26], tx[27], tx[28], tx[29], tx[30], tx[31], tx[32], tx[33], tx[34], tx[35], tx[36], tx[37], tx[38], tx[39], tx[40], tx[41], tx[42], tx[43], tx[44], tx[45], tx[46], hist_all + i * 16, ret_all + i * 16, dr_all + i * 640, tdx, ty, tdy);
  for (int k = 0; k < 96; ++k) { y[i*96+k] = ty[k]; dy[i*96+k] = tdy[k]; }
}
kernel void mk_hashemiEnv_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device const float* hist_all [[buffer(3)]], device const float* ret_all [[buffer(4)]], device const float* dr_all [[buffer(5)]], device float* olo [[buffer(6)]], device float* ohi [[buffer(7)]], device float* oL [[buffer(8)]], device const int* n [[buffer(9)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[47], thi[47], tsc[47], rlo[96], rhi[96], rL[96];
  for (int k = 0; k < 47; ++k) { tlo[k] = lo[i*47+k]; thi[k] = hi[i*47+k]; tsc[k] = sc[i*47+k]; }
  hk_hashemiEnv_box(hist_all + i * 16, ret_all + i * 16, dr_all + i * 640, tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 96; ++k) { olo[i*96+k] = rlo[k]; ohi[i*96+k] = rhi[k]; oL[i*96+k] = rL[k]; }
}
kernel void mk_hashemiEnvBeam_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device const float* dr_all [[buffer(2)]], device float* y [[buffer(3)]], device float* dy [[buffer(4)]], device const int* n [[buffer(5)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[33], tdx[33], ty[31], tdy[31];
  for (int k = 0; k < 33; ++k) { tx[k] = x[i*33+k]; tdx[k] = dx[i*33+k]; }
  hk_hashemiEnvBeam_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tx[15], tx[16], tx[17], tx[18], tx[19], tx[20], tx[21], tx[22], tx[23], tx[24], tx[25], tx[26], tx[27], tx[28], tx[29], tx[30], tx[31], tx[32], dr_all + i * 640, tdx, ty, tdy);
  for (int k = 0; k < 31; ++k) { y[i*31+k] = ty[k]; dy[i*31+k] = tdy[k]; }
}
kernel void mk_hashemiEnvBeam_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device const float* dr_all [[buffer(3)]], device float* olo [[buffer(4)]], device float* ohi [[buffer(5)]], device float* oL [[buffer(6)]], device const int* n [[buffer(7)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[33], thi[33], tsc[33], rlo[31], rhi[31], rL[31];
  for (int k = 0; k < 33; ++k) { tlo[k] = lo[i*33+k]; thi[k] = hi[i*33+k]; tsc[k] = sc[i*33+k]; }
  hk_hashemiEnvBeam_box(dr_all + i * 640, tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 31; ++k) { olo[i*31+k] = rlo[k]; ohi[i*31+k] = rhi[k]; oL[i*31+k] = rL[k]; }
}
kernel void mk_hashemiLoop_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device const float* W1_all [[buffer(2)]], device const float* b1_all [[buffer(3)]], device const float* W2_all [[buffer(4)]], device const float* hist_all [[buffer(5)]], device const float* ret_all [[buffer(6)]], device const float* dr_all [[buffer(7)]], device float* y [[buffer(8)]], device float* dy [[buffer(9)]], device const int* n [[buffer(10)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[52], tdx[52], ty[110], tdy[110];
  for (int k = 0; k < 52; ++k) { tx[k] = x[i*52+k]; tdx[k] = dx[i*52+k]; }
  hk_hashemiLoop_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tx[15], tx[16], tx[17], tx[18], tx[19], tx[20], tx[21], tx[22], tx[23], tx[24], tx[25], tx[26], tx[27], tx[28], tx[29], tx[30], tx[31], tx[32], tx[33], tx[34], tx[35], tx[36], tx[37], tx[38], tx[39], tx[40], tx[41], tx[42], tx[43], tx[44], tx[45], tx[46], tx[47], tx[48], tx[49], tx[50], tx[51], W1_all + i * 176, b1_all + i * 16, W2_all + i * 48, hist_all + i * 16, ret_all + i * 16, dr_all + i * 640, tdx, ty, tdy);
  for (int k = 0; k < 110; ++k) { y[i*110+k] = ty[k]; dy[i*110+k] = tdy[k]; }
}
kernel void mk_hashemiLoop_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device const float* W1_all [[buffer(3)]], device const float* b1_all [[buffer(4)]], device const float* W2_all [[buffer(5)]], device const float* hist_all [[buffer(6)]], device const float* ret_all [[buffer(7)]], device const float* dr_all [[buffer(8)]], device float* olo [[buffer(9)]], device float* ohi [[buffer(10)]], device float* oL [[buffer(11)]], device const int* n [[buffer(12)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[52], thi[52], tsc[52], rlo[110], rhi[110], rL[110];
  for (int k = 0; k < 52; ++k) { tlo[k] = lo[i*52+k]; thi[k] = hi[i*52+k]; tsc[k] = sc[i*52+k]; }
  hk_hashemiLoop_box(W1_all + i * 176, b1_all + i * 16, W2_all + i * 48, hist_all + i * 16, ret_all + i * 16, dr_all + i * 640, tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 110; ++k) { olo[i*110+k] = rlo[k]; ohi[i*110+k] = rhi[k]; oL[i*110+k] = rL[k]; }
}
kernel void mk_headToCmd_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_headToCmd_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_headToCmd_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_headToCmd_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_headToDriveAz_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_headToDriveAz_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_headToDriveAz_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_headToDriveAz_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_headToDriveEl_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_headToDriveEl_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_headToDriveEl_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_headToDriveEl_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_heatStep_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[13], tdx[13], ty[6], tdy[6];
  for (int k = 0; k < 13; ++k) { tx[k] = x[i*13+k]; tdx[k] = dx[i*13+k]; }
  hk_heatStep_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tdx, ty, tdy);
  for (int k = 0; k < 6; ++k) { y[i*6+k] = ty[k]; dy[i*6+k] = tdy[k]; }
}
kernel void mk_heatStep_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[13], thi[13], tsc[13], rlo[6], rhi[6], rL[6];
  for (int k = 0; k < 13; ++k) { tlo[k] = lo[i*13+k]; thi[k] = hi[i*13+k]; tsc[k] = sc[i*13+k]; }
  hk_heatStep_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 6; ++k) { olo[i*6+k] = rlo[k]; ohi[i*6+k] = rhi[k]; oL[i*6+k] = rL[k]; }
}
kernel void mk_helixAdvance_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_helixAdvance_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_helixAdvance_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_helixAdvance_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_hingeWrench_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[30], tdy[30];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_hingeWrench_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 30; ++k) { y[i*30+k] = ty[k]; dy[i*30+k] = tdy[k]; }
}
kernel void mk_hingeWrench_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[30], rhi[30], rL[30];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_hingeWrench_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 30; ++k) { olo[i*30+k] = rlo[k]; ohi[i*30+k] = rhi[k]; oL[i*30+k] = rL[k]; }
}
kernel void mk_hyperHit_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[11], tdx[11], ty[8], tdy[8];
  for (int k = 0; k < 11; ++k) { tx[k] = x[i*11+k]; tdx[k] = dx[i*11+k]; }
  hk_hyperHit_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tdx, ty, tdy);
  for (int k = 0; k < 8; ++k) { y[i*8+k] = ty[k]; dy[i*8+k] = tdy[k]; }
}
kernel void mk_hyperHit_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[11], thi[11], tsc[11], rlo[8], rhi[8], rL[8];
  for (int k = 0; k < 11; ++k) { tlo[k] = lo[i*11+k]; thi[k] = hi[i*11+k]; tsc[k] = sc[i*11+k]; }
  hk_hyperHit_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 8; ++k) { olo[i*8+k] = rlo[k]; ohi[i*8+k] = rhi[k]; oL[i*8+k] = rL[k]; }
}
kernel void mk_landAt_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[7], tdx[7], ty[2], tdy[2];
  for (int k = 0; k < 7; ++k) { tx[k] = x[i*7+k]; tdx[k] = dx[i*7+k]; }
  hk_landAt_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tdx, ty, tdy);
  for (int k = 0; k < 2; ++k) { y[i*2+k] = ty[k]; dy[i*2+k] = tdy[k]; }
}
kernel void mk_landAt_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[7], thi[7], tsc[7], rlo[2], rhi[2], rL[2];
  for (int k = 0; k < 7; ++k) { tlo[k] = lo[i*7+k]; thi[k] = hi[i*7+k]; tsc[k] = sc[i*7+k]; }
  hk_landAt_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 2; ++k) { olo[i*2+k] = rlo[k]; ohi[i*2+k] = rhi[k]; oL[i*2+k] = rL[k]; }
}
kernel void mk_lerp8_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[9], tdx[9], ty[1], tdy[1];
  for (int k = 0; k < 9; ++k) { tx[k] = x[i*9+k]; tdx[k] = dx[i*9+k]; }
  hk_lerp8_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_lerp8_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[9], thi[9], tsc[9], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 9; ++k) { tlo[k] = lo[i*9+k]; thi[k] = hi[i*9+k]; tsc[k] = sc[i*9+k]; }
  hk_lerp8_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_leverAt_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_leverAt_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_leverAt_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_leverAt_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_loopCap_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_loopCap_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_loopCap_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_loopCap_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_loopStep_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[7], tdx[7], ty[1], tdy[1];
  for (int k = 0; k < 7; ++k) { tx[k] = x[i*7+k]; tdx[k] = dx[i*7+k]; }
  hk_loopStep_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_loopStep_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[7], thi[7], tsc[7], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 7; ++k) { tlo[k] = lo[i*7+k]; thi[k] = hi[i*7+k]; tsc[k] = sc[i*7+k]; }
  hk_loopStep_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_lostSunS_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[1], tdy[1];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_lostSunS_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_lostSunS_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_lostSunS_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_megaGeom_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[17], tdx[17], ty[60], tdy[60];
  for (int k = 0; k < 17; ++k) { tx[k] = x[i*17+k]; tdx[k] = dx[i*17+k]; }
  hk_megaGeom_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tx[15], tx[16], tdx, ty, tdy);
  for (int k = 0; k < 60; ++k) { y[i*60+k] = ty[k]; dy[i*60+k] = tdy[k]; }
}
kernel void mk_megaGeom_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[17], thi[17], tsc[17], rlo[60], rhi[60], rL[60];
  for (int k = 0; k < 17; ++k) { tlo[k] = lo[i*17+k]; thi[k] = hi[i*17+k]; tsc[k] = sc[i*17+k]; }
  hk_megaGeom_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 60; ++k) { olo[i*60+k] = rlo[k]; ohi[i*60+k] = rhi[k]; oL[i*60+k] = rL[k]; }
}
kernel void mk_megaReqs_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[17], tdx[17], ty[7], tdy[7];
  for (int k = 0; k < 17; ++k) { tx[k] = x[i*17+k]; tdx[k] = dx[i*17+k]; }
  hk_megaReqs_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tx[15], tx[16], tdx, ty, tdy);
  for (int k = 0; k < 7; ++k) { y[i*7+k] = ty[k]; dy[i*7+k] = tdy[k]; }
}
kernel void mk_megaReqs_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[17], thi[17], tsc[17], rlo[7], rhi[7], rL[7];
  for (int k = 0; k < 17; ++k) { tlo[k] = lo[i*17+k]; thi[k] = hi[i*17+k]; tsc[k] = sc[i*17+k]; }
  hk_megaReqs_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 7; ++k) { olo[i*7+k] = rlo[k]; ohi[i*7+k] = rhi[k]; oL[i*7+k] = rL[k]; }
}
kernel void mk_megaScrew_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[17], tdx[17], ty[40], tdy[40];
  for (int k = 0; k < 17; ++k) { tx[k] = x[i*17+k]; tdx[k] = dx[i*17+k]; }
  hk_megaScrew_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tx[15], tx[16], tdx, ty, tdy);
  for (int k = 0; k < 40; ++k) { y[i*40+k] = ty[k]; dy[i*40+k] = tdy[k]; }
}
kernel void mk_megaScrew_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[17], thi[17], tsc[17], rlo[40], rhi[40], rL[40];
  for (int k = 0; k < 17; ++k) { tlo[k] = lo[i*17+k]; thi[k] = hi[i*17+k]; tsc[k] = sc[i*17+k]; }
  hk_megaScrew_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 40; ++k) { olo[i*40+k] = rlo[k]; ohi[i*40+k] = rhi[k]; oL[i*40+k] = rL[k]; }
}
kernel void mk_omega_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_omega_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_omega_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_omega_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_megaStep_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[17], tdx[17], ty[17], tdy[17];
  for (int k = 0; k < 17; ++k) { tx[k] = x[i*17+k]; tdx[k] = dx[i*17+k]; }
  hk_megaStep_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tx[15], tx[16], tdx, ty, tdy);
  for (int k = 0; k < 17; ++k) { y[i*17+k] = ty[k]; dy[i*17+k] = tdy[k]; }
}
kernel void mk_megaStep_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[17], thi[17], tsc[17], rlo[17], rhi[17], rL[17];
  for (int k = 0; k < 17; ++k) { tlo[k] = lo[i*17+k]; thi[k] = hi[i*17+k]; tsc[k] = sc[i*17+k]; }
  hk_megaStep_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 17; ++k) { olo[i*17+k] = rlo[k]; ohi[i*17+k] = rhi[k]; oL[i*17+k] = rL[k]; }
}
kernel void mk_megaThmsClosed_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[17], tdx[17], ty[56], tdy[56];
  for (int k = 0; k < 17; ++k) { tx[k] = x[i*17+k]; tdx[k] = dx[i*17+k]; }
  hk_megaThmsClosed_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tx[15], tx[16], tdx, ty, tdy);
  for (int k = 0; k < 56; ++k) { y[i*56+k] = ty[k]; dy[i*56+k] = tdy[k]; }
}
kernel void mk_megaThmsClosed_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[17], thi[17], tsc[17], rlo[56], rhi[56], rL[56];
  for (int k = 0; k < 17; ++k) { tlo[k] = lo[i*17+k]; thi[k] = hi[i*17+k]; tsc[k] = sc[i*17+k]; }
  hk_megaThmsClosed_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 56; ++k) { olo[i*56+k] = rlo[k]; ohi[i*56+k] = rhi[k]; oL[i*56+k] = rL[k]; }
}
kernel void mk_megaThmsState_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[17], tdx[17], ty[50], tdy[50];
  for (int k = 0; k < 17; ++k) { tx[k] = x[i*17+k]; tdx[k] = dx[i*17+k]; }
  hk_megaThmsState_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tx[15], tx[16], tdx, ty, tdy);
  for (int k = 0; k < 50; ++k) { y[i*50+k] = ty[k]; dy[i*50+k] = tdy[k]; }
}
kernel void mk_megaThmsState_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[17], thi[17], tsc[17], rlo[50], rhi[50], rL[50];
  for (int k = 0; k < 17; ++k) { tlo[k] = lo[i*17+k]; thi[k] = hi[i*17+k]; tsc[k] = sc[i*17+k]; }
  hk_megaThmsState_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 50; ++k) { olo[i*50+k] = rlo[k]; ohi[i*50+k] = rhi[k]; oL[i*50+k] = rL[k]; }
}
kernel void mk_mlpPolicy_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device const float* W1_all [[buffer(2)]], device const float* b1_all [[buffer(3)]], device const float* W2_all [[buffer(4)]], device float* y [[buffer(5)]], device float* dy [[buffer(6)]], device const int* n [[buffer(7)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[14], tdx[14], ty[3], tdy[3];
  for (int k = 0; k < 14; ++k) { tx[k] = x[i*14+k]; tdx[k] = dx[i*14+k]; }
  hk_mlpPolicy_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], W1_all + i * 176, b1_all + i * 16, W2_all + i * 48, tdx, ty, tdy);
  for (int k = 0; k < 3; ++k) { y[i*3+k] = ty[k]; dy[i*3+k] = tdy[k]; }
}
kernel void mk_mlpPolicy_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device const float* W1_all [[buffer(3)]], device const float* b1_all [[buffer(4)]], device const float* W2_all [[buffer(5)]], device float* olo [[buffer(6)]], device float* ohi [[buffer(7)]], device float* oL [[buffer(8)]], device const int* n [[buffer(9)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[14], thi[14], tsc[14], rlo[3], rhi[3], rL[3];
  for (int k = 0; k < 14; ++k) { tlo[k] = lo[i*14+k]; thi[k] = hi[i*14+k]; tsc[k] = sc[i*14+k]; }
  hk_mlpPolicy_box(W1_all + i * 176, b1_all + i * 16, W2_all + i * 48, tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 3; ++k) { olo[i*3+k] = rlo[k]; ohi[i*3+k] = rhi[k]; oL[i*3+k] = rL[k]; }
}
kernel void mk_ntuOf_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_ntuOf_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_ntuOf_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_ntuOf_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_nusseltOf_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_nusseltOf_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_nusseltOf_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_nusseltOf_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_nusseltTurb_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_nusseltTurb_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_nusseltTurb_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_nusseltTurb_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_obsOf_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[11], tdx[11], ty[11], tdy[11];
  for (int k = 0; k < 11; ++k) { tx[k] = x[i*11+k]; tdx[k] = dx[i*11+k]; }
  hk_obsOf_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tdx, ty, tdy);
  for (int k = 0; k < 11; ++k) { y[i*11+k] = ty[k]; dy[i*11+k] = tdy[k]; }
}
kernel void mk_obsOf_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[11], thi[11], tsc[11], rlo[11], rhi[11], rL[11];
  for (int k = 0; k < 11; ++k) { tlo[k] = lo[i*11+k]; thi[k] = hi[i*11+k]; tsc[k] = sc[i*11+k]; }
  hk_obsOf_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 11; ++k) { olo[i*11+k] = rlo[k]; ohi[i*11+k] = rhi[k]; oL[i*11+k] = rL[k]; }
}
kernel void mk_oilCp_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_oilCp_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_oilCp_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_oilCp_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_oilK_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_oilK_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_oilK_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_oilK_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_oilMu_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_oilMu_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_oilMu_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_oilMu_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_oilRho_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_oilRho_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_oilRho_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_oilRho_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_oilStep_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[13], tdx[13], ty[1], tdy[1];
  for (int k = 0; k < 13; ++k) { tx[k] = x[i*13+k]; tdx[k] = dx[i*13+k]; }
  hk_oilStep_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_oilStep_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[13], thi[13], tsc[13], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 13; ++k) { tlo[k] = lo[i*13+k]; thi[k] = hi[i*13+k]; tsc[k] = sc[i*13+k]; }
  hk_oilStep_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_pipeArea_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_pipeArea_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_pipeArea_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_pipeArea_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_pipeGreen_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_pipeGreen_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_pipeGreen_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_pipeGreen_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_pointVel_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[9], tdx[9], ty[3], tdy[3];
  for (int k = 0; k < 9; ++k) { tx[k] = x[i*9+k]; tdx[k] = dx[i*9+k]; }
  hk_pointVel_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tdx, ty, tdy);
  for (int k = 0; k < 3; ++k) { y[i*3+k] = ty[k]; dy[i*3+k] = tdy[k]; }
}
kernel void mk_pointVel_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[9], thi[9], tsc[9], rlo[3], rhi[3], rL[3];
  for (int k = 0; k < 9; ++k) { tlo[k] = lo[i*9+k]; thi[k] = hi[i*9+k]; tsc[k] = sc[i*9+k]; }
  hk_pointVel_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 3; ++k) { olo[i*3+k] = rlo[k]; ohi[i*3+k] = rhi[k]; oL[i*3+k] = rL[k]; }
}
kernel void mk_pointingError_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_pointingError_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_pointingError_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_pointingError_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_postTop_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[13], tdx[13], ty[3], tdy[3];
  for (int k = 0; k < 13; ++k) { tx[k] = x[i*13+k]; tdx[k] = dx[i*13+k]; }
  hk_postTop_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tdx, ty, tdy);
  for (int k = 0; k < 3; ++k) { y[i*3+k] = ty[k]; dy[i*3+k] = tdy[k]; }
}
kernel void mk_postTop_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[13], thi[13], tsc[13], rlo[3], rhi[3], rL[3];
  for (int k = 0; k < 13; ++k) { tlo[k] = lo[i*13+k]; thi[k] = hi[i*13+k]; tsc[k] = sc[i*13+k]; }
  hk_postTop_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 3; ++k) { olo[i*3+k] = rlo[k]; ohi[i*3+k] = rhi[k]; oL[i*3+k] = rL[k]; }
}
kernel void mk_prandtl_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_prandtl_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prandtl_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_prandtl_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_pulleyAt_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[2], tdy[2];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_pulleyAt_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 2; ++k) { y[i*2+k] = ty[k]; dy[i*2+k] = tdy[k]; }
}
kernel void mk_pulleyAt_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[2], rhi[2], rL[2];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_pulleyAt_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 2; ++k) { olo[i*2+k] = rlo[k]; ohi[i*2+k] = rhi[k]; oL[i*2+k] = rL[k]; }
}
kernel void mk_pumpCmd_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_pumpCmd_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_pumpCmd_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_pumpCmd_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_pumpCostRaw_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_pumpCostRaw_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_pumpCostRaw_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_pumpCostRaw_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_pumpElec_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[1], tdy[1];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_pumpElec_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_pumpElec_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_pumpElec_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_pumpHyd_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_pumpHyd_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_pumpHyd_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_pumpHyd_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_qAbs_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_qAbs_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_qAbs_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_qAbs_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_qCoilLoss_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_qCoilLoss_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_qCoilLoss_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_qCoilLoss_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_qCoilLossW_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_qCoilLossW_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_qCoilLossW_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_qCoilLossW_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_qNet_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[10], tdx[10], ty[1], tdy[1];
  for (int k = 0; k < 10; ++k) { tx[k] = x[i*10+k]; tdx[k] = dx[i*10+k]; }
  hk_qNet_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_qNet_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[10], thi[10], tsc[10], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 10; ++k) { tlo[k] = lo[i*10+k]; thi[k] = hi[i*10+k]; tsc[k] = sc[i*10+k]; }
  hk_qNet_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_qPipe_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_qPipe_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_qPipe_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_qPipe_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_qPot_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_qPot_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_qPot_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_qPot_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_radCeil_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_radCeil_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_radCeil_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_radCeil_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_recip_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[1], tdy[1];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_recip_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_recip_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_recip_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_reflect3_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[3], tdy[3];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_reflect3_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 3; ++k) { y[i*3+k] = ty[k]; dy[i*3+k] = tdy[k]; }
}
kernel void mk_reflect3_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[3], rhi[3], rL[3];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_reflect3_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 3; ++k) { olo[i*3+k] = rlo[k]; ohi[i*3+k] = rhi[k]; oL[i*3+k] = rL[k]; }
}
kernel void mk_rewardShapeRaw_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[1], tdy[1];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_rewardShapeRaw_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_rewardShapeRaw_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_rewardShapeRaw_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_rewardStep_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[5], tdy[5];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_rewardStep_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 5; ++k) { y[i*5+k] = ty[k]; dy[i*5+k] = tdy[k]; }
}
kernel void mk_rewardStep_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[5], rhi[5], rL[5];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_rewardStep_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 5; ++k) { olo[i*5+k] = rlo[k]; ohi[i*5+k] = rhi[k]; oL[i*5+k] = rL[k]; }
}
kernel void mk_reynolds_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_reynolds_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_reynolds_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_reynolds_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_rollY_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[6], tdy[6];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_rollY_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 6; ++k) { y[i*6+k] = ty[k]; dy[i*6+k] = tdy[k]; }
}
kernel void mk_rollY_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[6], rhi[6], rL[6];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_rollY_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 6; ++k) { olo[i*6+k] = rlo[k]; ohi[i*6+k] = rhi[k]; oL[i*6+k] = rL[k]; }
}
kernel void mk_rollerRadius_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[1], tdy[1];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_rollerRadius_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_rollerRadius_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_rollerRadius_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_rot_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[2], tdy[2];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_rot_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 2; ++k) { y[i*2+k] = ty[k]; dy[i*2+k] = tdy[k]; }
}
kernel void mk_rot_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[2], rhi[2], rL[2];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_rot_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 2; ++k) { olo[i*2+k] = rlo[k]; ohi[i*2+k] = rhi[k]; oL[i*2+k] = rL[k]; }
}
kernel void mk_rotz_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[3], tdy[3];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_rotz_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 3; ++k) { y[i*3+k] = ty[k]; dy[i*3+k] = tdy[k]; }
}
kernel void mk_rotz_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[3], rhi[3], rL[3];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_rotz_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 3; ++k) { olo[i*3+k] = rlo[k]; ohi[i*3+k] = rhi[k]; oL[i*3+k] = rL[k]; }
}
kernel void mk_sampleRay_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[7], tdy[7];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_sampleRay_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 7; ++k) { y[i*7+k] = ty[k]; dy[i*7+k] = tdy[k]; }
}
kernel void mk_sampleRay_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[7], rhi[7], rL[7];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_sampleRay_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 7; ++k) { olo[i*7+k] = rlo[k]; ohi[i*7+k] = rhi[k]; oL[i*7+k] = rL[k]; }
}
kernel void mk_screwLength_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_screwLength_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_screwLength_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_screwLength_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_screwTwist_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[6], tdy[6];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_screwTwist_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 6; ++k) { y[i*6+k] = ty[k]; dy[i*6+k] = tdy[k]; }
}
kernel void mk_screwTwist_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[6], rhi[6], rL[6];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_screwTwist_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 6; ++k) { olo[i*6+k] = rlo[k]; ohi[i*6+k] = rhi[k]; oL[i*6+k] = rL[k]; }
}
kernel void mk_screwWrench_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[30], tdy[30];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_screwWrench_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 30; ++k) { y[i*30+k] = ty[k]; dy[i*30+k] = tdy[k]; }
}
kernel void mk_screwWrench_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[30], rhi[30], rL[30];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_screwWrench_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 30; ++k) { olo[i*30+k] = rlo[k]; ohi[i*30+k] = rhi[k]; oL[i*30+k] = rL[k]; }
}
kernel void mk_secondaryMag_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_secondaryMag_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_secondaryMag_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_secondaryMag_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_setLength_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_setLength_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_setLength_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_setLength_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_shift_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device const float* h_all [[buffer(2)]], device float* y [[buffer(3)]], device float* dy [[buffer(4)]], device const int* n [[buffer(5)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[16], tdy[16];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_shift_jvp(tx[0], h_all + i * 16, tdx, ty, tdy);
  for (int k = 0; k < 16; ++k) { y[i*16+k] = ty[k]; dy[i*16+k] = tdy[k]; }
}
kernel void mk_shift_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device const float* h_all [[buffer(3)]], device float* olo [[buffer(4)]], device float* ohi [[buffer(5)]], device float* oL [[buffer(6)]], device const int* n [[buffer(7)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[16], rhi[16], rL[16];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_shift_box(h_all + i * 16, tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 16; ++k) { olo[i*16+k] = rlo[k]; ohi[i*16+k] = rhi[k]; oL[i*16+k] = rL[k]; }
}
kernel void mk_slackSpot_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_slackSpot_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_slackSpot_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_slackSpot_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_slotExit_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_slotExit_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_slotExit_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_slotExit_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_sphereBestFocus_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_sphereBestFocus_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_sphereBestFocus_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_sphereBestFocus_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_sphereBlur_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_sphereBlur_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_sphereBlur_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_sphereBlur_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_sphereDev_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_sphereDev_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_sphereDev_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_sphereDev_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_sphereFocal_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_sphereFocal_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_sphereFocal_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_sphereFocal_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_sphereHit_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[7], tdx[7], ty[6], tdy[6];
  for (int k = 0; k < 7; ++k) { tx[k] = x[i*7+k]; tdx[k] = dx[i*7+k]; }
  hk_sphereHit_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tdx, ty, tdy);
  for (int k = 0; k < 6; ++k) { y[i*6+k] = ty[k]; dy[i*6+k] = tdy[k]; }
}
kernel void mk_sphereHit_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[7], thi[7], tsc[7], rlo[6], rhi[6], rL[6];
  for (int k = 0; k < 7; ++k) { tlo[k] = lo[i*7+k]; thi[k] = hi[i*7+k]; tsc[k] = sc[i*7+k]; }
  hk_sphereHit_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 6; ++k) { olo[i*6+k] = rlo[k]; ohi[i*6+k] = rhi[k]; oL[i*6+k] = rL[k]; }
}
kernel void mk_step_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[16], tdx[16], ty[8], tdy[8];
  for (int k = 0; k < 16; ++k) { tx[k] = x[i*16+k]; tdx[k] = dx[i*16+k]; }
  hk_step_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tx[15], tdx, ty, tdy);
  for (int k = 0; k < 8; ++k) { y[i*8+k] = ty[k]; dy[i*8+k] = tdy[k]; }
}
kernel void mk_step_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[16], thi[16], tsc[16], rlo[8], rhi[8], rL[8];
  for (int k = 0; k < 16; ++k) { tlo[k] = lo[i*16+k]; thi[k] = hi[i*16+k]; tsc[k] = sc[i*16+k]; }
  hk_step_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 8; ++k) { olo[i*8+k] = rlo[k]; ohi[i*8+k] = rhi[k]; oL[i*8+k] = rL[k]; }
}
kernel void mk_strutStrain_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[9], tdx[9], ty[1], tdy[1];
  for (int k = 0; k < 9; ++k) { tx[k] = x[i*9+k]; tdx[k] = dx[i*9+k]; }
  hk_strutStrain_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_strutStrain_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[9], thi[9], tsc[9], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 9; ++k) { tlo[k] = lo[i*9+k]; thi[k] = hi[i*9+k]; tsc[k] = sc[i*9+k]; }
  hk_strutStrain_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_sunDir_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[3], tdy[3];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_sunDir_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 3; ++k) { y[i*3+k] = ty[k]; dy[i*3+k] = tdy[k]; }
}
kernel void mk_sunDir_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[3], rhi[3], rL[3];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_sunDir_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 3; ++k) { olo[i*3+k] = rlo[k]; ohi[i*3+k] = rhi[k]; oL[i*3+k] = rL[k]; }
}
kernel void mk_sunInDish_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[3], tdy[3];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_sunInDish_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 3; ++k) { y[i*3+k] = ty[k]; dy[i*3+k] = tdy[k]; }
}
kernel void mk_sunInDish_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[3], rhi[3], rL[3];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_sunInDish_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 3; ++k) { olo[i*3+k] = rlo[k]; ohi[i*3+k] = rhi[k]; oL[i*3+k] = rL[k]; }
}
kernel void mk_sunReachableS_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_sunReachableS_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_sunReachableS_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_sunReachableS_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_swingFocus_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[2], tdy[2];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_swingFocus_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 2; ++k) { y[i*2+k] = ty[k]; dy[i*2+k] = tdy[k]; }
}
kernel void mk_swingFocus_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[2], rhi[2], rL[2];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_swingFocus_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 2; ++k) { olo[i*2+k] = rlo[k]; ohi[i*2+k] = rhi[k]; oL[i*2+k] = rL[k]; }
}
kernel void mk_swingNormal_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[2], tdy[2];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_swingNormal_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 2; ++k) { y[i*2+k] = ty[k]; dy[i*2+k] = tdy[k]; }
}
kernel void mk_swingNormal_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[2], rhi[2], rL[2];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_swingNormal_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 2; ++k) { olo[i*2+k] = rlo[k]; ohi[i*2+k] = rhi[k]; oL[i*2+k] = rL[k]; }
}
kernel void mk_swingOfLength_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[1], tdy[1];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_swingOfLength_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_swingOfLength_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_swingOfLength_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_swingTwist_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[6], tdy[6];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_swingTwist_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 6; ++k) { y[i*6+k] = ty[k]; dy[i*6+k] = tdy[k]; }
}
kernel void mk_swingTwist_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[6], rhi[6], rL[6];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_swingTwist_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 6; ++k) { olo[i*6+k] = rlo[k]; ohi[i*6+k] = rhi[k]; oL[i*6+k] = rL[k]; }
}
kernel void mk_swingVertex_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[2], tdy[2];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_swingVertex_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 2; ++k) { y[i*2+k] = ty[k]; dy[i*2+k] = tdy[k]; }
}
kernel void mk_swingVertex_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[2], rhi[2], rL[2];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_swingVertex_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 2; ++k) { olo[i*2+k] = rlo[k]; ohi[i*2+k] = rhi[k]; oL[i*2+k] = rL[k]; }
}
kernel void mk_swingVertexAt_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[2], tdy[2];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_swingVertexAt_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 2; ++k) { y[i*2+k] = ty[k]; dy[i*2+k] = tdy[k]; }
}
kernel void mk_swingVertexAt_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[2], rhi[2], rL[2];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_swingVertexAt_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 2; ++k) { olo[i*2+k] = rlo[k]; ohi[i*2+k] = rhi[k]; oL[i*2+k] = rL[k]; }
}
kernel void mk_swungPt_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[2], tdy[2];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_swungPt_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 2; ++k) { y[i*2+k] = ty[k]; dy[i*2+k] = tdy[k]; }
}
kernel void mk_swungPt_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[2], rhi[2], rL[2];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_swungPt_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 2; ++k) { olo[i*2+k] = rlo[k]; ohi[i*2+k] = rhi[k]; oL[i*2+k] = rL[k]; }
}
kernel void mk_tilt_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[3], tdy[3];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_tilt_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 3; ++k) { y[i*3+k] = ty[k]; dy[i*3+k] = tdy[k]; }
}
kernel void mk_tilt_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[3], rhi[3], rL[3];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_tilt_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 3; ++k) { olo[i*3+k] = rlo[k]; ohi[i*3+k] = rhi[k]; oL[i*3+k] = rL[k]; }
}
kernel void mk_tiltOfMismatch_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_tiltOfMismatch_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_tiltOfMismatch_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_tiltOfMismatch_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_traceBeam_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[18], tdx[18], ty[8], tdy[8];
  for (int k = 0; k < 18; ++k) { tx[k] = x[i*18+k]; tdx[k] = dx[i*18+k]; }
  hk_traceBeam_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tx[15], tx[16], tx[17], tdx, ty, tdy);
  for (int k = 0; k < 8; ++k) { y[i*8+k] = ty[k]; dy[i*8+k] = tdy[k]; }
}
kernel void mk_traceBeam_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[18], thi[18], tsc[18], rlo[8], rhi[8], rL[8];
  for (int k = 0; k < 18; ++k) { tlo[k] = lo[i*18+k]; thi[k] = hi[i*18+k]; tsc[k] = sc[i*18+k]; }
  hk_traceBeam_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 8; ++k) { olo[i*8+k] = rlo[k]; ohi[i*8+k] = rhi[k]; oL[i*8+k] = rL[k]; }
}
kernel void mk_traceConic_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[9], tdx[9], ty[9], tdy[9];
  for (int k = 0; k < 9; ++k) { tx[k] = x[i*9+k]; tdx[k] = dx[i*9+k]; }
  hk_traceConic_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tdx, ty, tdy);
  for (int k = 0; k < 9; ++k) { y[i*9+k] = ty[k]; dy[i*9+k] = tdy[k]; }
}
kernel void mk_traceConic_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[9], thi[9], tsc[9], rlo[9], rhi[9], rL[9];
  for (int k = 0; k < 9; ++k) { tlo[k] = lo[i*9+k]; thi[k] = hi[i*9+k]; tsc[k] = sc[i*9+k]; }
  hk_traceConic_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 9; ++k) { olo[i*9+k] = rlo[k]; ohi[i*9+k] = rhi[k]; oL[i*9+k] = rL[k]; }
}
kernel void mk_traceFacet_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[10], tdx[10], ty[5], tdy[5];
  for (int k = 0; k < 10; ++k) { tx[k] = x[i*10+k]; tdx[k] = dx[i*10+k]; }
  hk_traceFacet_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tdx, ty, tdy);
  for (int k = 0; k < 5; ++k) { y[i*5+k] = ty[k]; dy[i*5+k] = tdy[k]; }
}
kernel void mk_traceFacet_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[10], thi[10], tsc[10], rlo[5], rhi[5], rL[5];
  for (int k = 0; k < 10; ++k) { tlo[k] = lo[i*10+k]; thi[k] = hi[i*10+k]; tsc[k] = sc[i*10+k]; }
  hk_traceFacet_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 5; ++k) { olo[i*5+k] = rlo[k]; ohi[i*5+k] = rhi[k]; oL[i*5+k] = rL[k]; }
}
kernel void mk_traceRay_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[8], tdy[8];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_traceRay_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 8; ++k) { y[i*8+k] = ty[k]; dy[i*8+k] = tdy[k]; }
}
kernel void mk_traceRay_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[8], rhi[8], rL[8];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_traceRay_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 8; ++k) { olo[i*8+k] = rlo[k]; ohi[i*8+k] = rhi[k]; oL[i*8+k] = rL[k]; }
}
kernel void mk_traceRayErr_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[18], tdx[18], ty[8], tdy[8];
  for (int k = 0; k < 18; ++k) { tx[k] = x[i*18+k]; tdx[k] = dx[i*18+k]; }
  hk_traceRayErr_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tx[15], tx[16], tx[17], tdx, ty, tdy);
  for (int k = 0; k < 8; ++k) { y[i*8+k] = ty[k]; dy[i*8+k] = tdy[k]; }
}
kernel void mk_traceRayErr_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[18], thi[18], tsc[18], rlo[8], rhi[8], rL[8];
  for (int k = 0; k < 18; ++k) { tlo[k] = lo[i*18+k]; thi[k] = hi[i*18+k]; tsc[k] = sc[i*18+k]; }
  hk_traceRayErr_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 8; ++k) { olo[i*8+k] = rlo[k]; ohi[i*8+k] = rhi[k]; oL[i*8+k] = rL[k]; }
}
kernel void mk_traceRayK_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[13], tdx[13], ty[8], tdy[8];
  for (int k = 0; k < 13; ++k) { tx[k] = x[i*13+k]; tdx[k] = dx[i*13+k]; }
  hk_traceRayK_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tdx, ty, tdy);
  for (int k = 0; k < 8; ++k) { y[i*8+k] = ty[k]; dy[i*8+k] = tdy[k]; }
}
kernel void mk_traceRayK_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[13], thi[13], tsc[13], rlo[8], rhi[8], rL[8];
  for (int k = 0; k < 13; ++k) { tlo[k] = lo[i*13+k]; thi[k] = hi[i*13+k]; tsc[k] = sc[i*13+k]; }
  hk_traceRayK_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 8; ++k) { olo[i*8+k] = rlo[k]; ohi[i*8+k] = rhi[k]; oL[i*8+k] = rL[k]; }
}
kernel void mk_traceRayKErr_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[19], tdx[19], ty[8], tdy[8];
  for (int k = 0; k < 19; ++k) { tx[k] = x[i*19+k]; tdx[k] = dx[i*19+k]; }
  hk_traceRayKErr_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tx[13], tx[14], tx[15], tx[16], tx[17], tx[18], tdx, ty, tdy);
  for (int k = 0; k < 8; ++k) { y[i*8+k] = ty[k]; dy[i*8+k] = tdy[k]; }
}
kernel void mk_traceRayKErr_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[19], thi[19], tsc[19], rlo[8], rhi[8], rL[8];
  for (int k = 0; k < 19; ++k) { tlo[k] = lo[i*19+k]; thi[k] = hi[i*19+k]; tsc[k] = sc[i*19+k]; }
  hk_traceRayKErr_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 8; ++k) { olo[i*8+k] = rlo[k]; ohi[i*8+k] = rhi[k]; oL[i*8+k] = rL[k]; }
}
kernel void mk_traceSphere_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[8], tdx[8], ty[5], tdy[5];
  for (int k = 0; k < 8; ++k) { tx[k] = x[i*8+k]; tdx[k] = dx[i*8+k]; }
  hk_traceSphere_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tdx, ty, tdy);
  for (int k = 0; k < 5; ++k) { y[i*5+k] = ty[k]; dy[i*5+k] = tdy[k]; }
}
kernel void mk_traceSphere_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[8], thi[8], tsc[8], rlo[5], rhi[5], rL[5];
  for (int k = 0; k < 8; ++k) { tlo[k] = lo[i*8+k]; thi[k] = hi[i*8+k]; tsc[k] = sc[i*8+k]; }
  hk_traceSphere_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 5; ++k) { olo[i*5+k] = rlo[k]; ohi[i*5+k] = rhi[k]; oL[i*5+k] = rL[k]; }
}
kernel void mk_transitTime_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_transitTime_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_transitTime_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_transitTime_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_turnLoss_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_turnLoss_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_turnLoss_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_turnLoss_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_turnOut_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[8], tdx[8], ty[1], tdy[1];
  for (int k = 0; k < 8; ++k) { tx[k] = x[i*8+k]; tdx[k] = dx[i*8+k]; }
  hk_turnOut_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_turnOut_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[8], thi[8], tsc[8], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 8; ++k) { tlo[k] = lo[i*8+k]; thi[k] = hi[i*8+k]; tsc[k] = sc[i*8+k]; }
  hk_turnOut_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_uPipeCyl_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_uPipeCyl_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_uPipeCyl_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_uPipeCyl_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_uaOf_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_uaOf_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_uaOf_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_uaOf_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_unit3_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[3], tdy[3];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_unit3_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 3; ++k) { y[i*3+k] = ty[k]; dy[i*3+k] = tdy[k]; }
}
kernel void mk_unit3_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[3], rhi[3], rL[3];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_unit3_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 3; ++k) { olo[i*3+k] = rlo[k]; ohi[i*3+k] = rhi[k]; oL[i*3+k] = rL[k]; }
}
kernel void mk_velOf_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_velOf_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_velOf_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_velOf_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_wBearX_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[6], tdy[6];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_wBearX_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 6; ++k) { y[i*6+k] = ty[k]; dy[i*6+k] = tdy[k]; }
}
kernel void mk_wBearX_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[6], rhi[6], rL[6];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_wBearX_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 6; ++k) { olo[i*6+k] = rlo[k]; ohi[i*6+k] = rhi[k]; oL[i*6+k] = rL[k]; }
}
kernel void mk_wBearY_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[6], tdy[6];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_wBearY_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 6; ++k) { y[i*6+k] = ty[k]; dy[i*6+k] = tdy[k]; }
}
kernel void mk_wBearY_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[6], rhi[6], rL[6];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_wBearY_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 6; ++k) { olo[i*6+k] = rlo[k]; ohi[i*6+k] = rhi[k]; oL[i*6+k] = rL[k]; }
}
kernel void mk_wDrive_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[13], tdx[13], ty[6], tdy[6];
  for (int k = 0; k < 13; ++k) { tx[k] = x[i*13+k]; tdx[k] = dx[i*13+k]; }
  hk_wDrive_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tdx, ty, tdy);
  for (int k = 0; k < 6; ++k) { y[i*6+k] = ty[k]; dy[i*6+k] = tdy[k]; }
}
kernel void mk_wDrive_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[13], thi[13], tsc[13], rlo[6], rhi[6], rL[6];
  for (int k = 0; k < 13; ++k) { tlo[k] = lo[i*13+k]; thi[k] = hi[i*13+k]; tsc[k] = sc[i*13+k]; }
  hk_wDrive_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 6; ++k) { olo[i*6+k] = rlo[k]; ohi[i*6+k] = rhi[k]; oL[i*6+k] = rL[k]; }
}
kernel void mk_wRollN_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[6], tdy[6];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_wRollN_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 6; ++k) { y[i*6+k] = ty[k]; dy[i*6+k] = tdy[k]; }
}
kernel void mk_wRollN_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[6], rhi[6], rL[6];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_wRollN_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 6; ++k) { olo[i*6+k] = rlo[k]; ohi[i*6+k] = rhi[k]; oL[i*6+k] = rL[k]; }
}
kernel void mk_wRollNr_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[6], tdy[6];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_wRollNr_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 6; ++k) { y[i*6+k] = ty[k]; dy[i*6+k] = tdy[k]; }
}
kernel void mk_wRollNr_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[6], rhi[6], rL[6];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_wRollNr_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 6; ++k) { olo[i*6+k] = rlo[k]; ohi[i*6+k] = rhi[k]; oL[i*6+k] = rL[k]; }
}
kernel void mk_wRollP_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[6], tdy[6];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_wRollP_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 6; ++k) { y[i*6+k] = ty[k]; dy[i*6+k] = tdy[k]; }
}
kernel void mk_wRollP_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[6], rhi[6], rL[6];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_wRollP_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 6; ++k) { olo[i*6+k] = rlo[k]; ohi[i*6+k] = rhi[k]; oL[i*6+k] = rL[k]; }
}
kernel void mk_wRollPr_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[6], tdy[6];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_wRollPr_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 6; ++k) { y[i*6+k] = ty[k]; dy[i*6+k] = tdy[k]; }
}
kernel void mk_wRollPr_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[6], rhi[6], rL[6];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_wRollPr_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 6; ++k) { olo[i*6+k] = rlo[k]; ohi[i*6+k] = rhi[k]; oL[i*6+k] = rL[k]; }
}
kernel void mk_wStop_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[6], tdy[6];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_wStop_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 6; ++k) { y[i*6+k] = ty[k]; dy[i*6+k] = tdy[k]; }
}
kernel void mk_wStop_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[6], rhi[6], rL[6];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_wStop_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 6; ++k) { olo[i*6+k] = rlo[k]; ohi[i*6+k] = rhi[k]; oL[i*6+k] = rL[k]; }
}
kernel void mk_wallTemp_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_wallTemp_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_wallTemp_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_wallTemp_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_wireLen_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_wireLen_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_wireLen_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_wireLen_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_wireLever_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_wireLever_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_wireLever_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_wireLever_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_wireTension_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_wireTension_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_wireTension_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_wireTension_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_wrapRad_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_wrapRad_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_wrapRad_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_wrapRad_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_wrenchAt_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[6], tdy[6];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_wrenchAt_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 6; ++k) { y[i*6+k] = ty[k]; dy[i*6+k] = tdy[k]; }
}
kernel void mk_wrenchAt_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[6], rhi[6], rL[6];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_wrenchAt_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 6; ++k) { olo[i*6+k] = rlo[k]; ohi[i*6+k] = rhi[k]; oL[i*6+k] = rL[k]; }
}
kernel void mk_Fits_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[1], tdy[1];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_Fits_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_Fits_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_Fits_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_FitsReceiver_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_FitsReceiver_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_FitsReceiver_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_FitsReceiver_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_HangerClearsPost_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_HangerClearsPost_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_HangerClearsPost_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_HangerClearsPost_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_HoldsDish_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_HoldsDish_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_HoldsDish_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_HoldsDish_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_LostSun_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[1], tdy[1];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_LostSun_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_LostSun_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_LostSun_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_MastClears_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_MastClears_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_MastClears_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_MastClears_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_PumpWithinBudget_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_PumpWithinBudget_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_PumpWithinBudget_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_PumpWithinBudget_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_ReachesVertical_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_ReachesVertical_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_ReachesVertical_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_ReachesVertical_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_SlackHarmless_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_SlackHarmless_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_SlackHarmless_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_SlackHarmless_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_SunReachable_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_SunReachable_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_SunReachable_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_SunReachable_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_TankHolds_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_TankHolds_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_TankHolds_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_TankHolds_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_TrackerBudget_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_TrackerBudget_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_TrackerBudget_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_TrackerBudget_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_azRate_pos_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_prop_azRate_pos_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_azRate_pos_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_prop_azRate_pos_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_bearing_life_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_prop_bearing_life_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_bearing_life_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_prop_bearing_life_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_cable_drop_small_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_prop_cable_drop_small_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_cable_drop_small_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_prop_cable_drop_small_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_captureS_slope_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_prop_captureS_slope_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_captureS_slope_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_prop_captureS_slope_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_clearance_hashemi_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_prop_clearance_hashemi_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_clearance_hashemi_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_prop_clearance_hashemi_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_conicZ_paraboloid_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_prop_conicZ_paraboloid_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_conicZ_paraboloid_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_prop_conicZ_paraboloid_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_conicZ_sphere_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_prop_conicZ_sphere_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_conicZ_sphere_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_prop_conicZ_sphere_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_constraints_reciprocal_yaw_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[1], tdy[1];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_prop_constraints_reciprocal_yaw_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_constraints_reciprocal_yaw_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_prop_constraints_reciprocal_yaw_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_dishAxes_rot_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_prop_dishAxes_rot_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_dishAxes_rot_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_prop_dishAxes_rot_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_drive_recip_yaw_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[13], tdx[13], ty[1], tdy[1];
  for (int k = 0; k < 13; ++k) { tx[k] = x[i*13+k]; tdx[k] = dx[i*13+k]; }
  hk_prop_drive_recip_yaw_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_drive_recip_yaw_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[13], thi[13], tsc[13], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 13; ++k) { tlo[k] = lo[i*13+k]; thi[k] = hi[i*13+k]; tsc[k] = sc[i*13+k]; }
  hk_prop_drive_recip_yaw_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_drive_works_on_yaw_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[13], tdx[13], ty[1], tdy[1];
  for (int k = 0; k < 13; ++k) { tx[k] = x[i*13+k]; tdx[k] = dx[i*13+k]; }
  hk_prop_drive_works_on_yaw_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_drive_works_on_yaw_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[13], thi[13], tsc[13], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 13; ++k) { tlo[k] = lo[i*13+k]; thi[k] = hi[i*13+k]; tsc[k] = sc[i*13+k]; }
  hk_prop_drive_works_on_yaw_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_edgeClip_cross_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_prop_edgeClip_cross_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_edgeClip_cross_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_prop_edgeClip_cross_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_edgeClip_radius_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_prop_edgeClip_radius_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_edgeClip_radius_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_prop_edgeClip_radius_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_edgeClip_radius_hashemi_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_prop_edgeClip_radius_hashemi_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_edgeClip_radius_hashemi_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_prop_edgeClip_radius_hashemi_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_edgeClip_reach_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_prop_edgeClip_reach_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_edgeClip_reach_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_prop_edgeClip_reach_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_edgeDepth_horizon_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_prop_edgeDepth_horizon_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_edgeDepth_horizon_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_prop_edgeDepth_horizon_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_edgeDepth_le_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_prop_edgeDepth_le_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_edgeDepth_le_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_prop_edgeDepth_le_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_edgeDepth_noon_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_prop_edgeDepth_noon_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_edgeDepth_noon_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_prop_edgeDepth_noon_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_edgeLever_dead_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_prop_edgeLever_dead_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_edgeLever_dead_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_prop_edgeLever_dead_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_edgeLever_pos_iff_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_prop_edgeLever_pos_iff_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_edgeLever_pos_iff_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_prop_edgeLever_pos_iff_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_elPower_eq_wire_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_prop_elPower_eq_wire_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_elPower_eq_wire_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_prop_elPower_eq_wire_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_elPower_le_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_prop_elPower_le_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_elPower_le_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_prop_elPower_le_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_focus_on_axis_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_prop_focus_on_axis_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_focus_on_axis_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_prop_focus_on_axis_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_grooved_reciprocal_yaw_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[1], tdy[1];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_prop_grooved_reciprocal_yaw_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_grooved_reciprocal_yaw_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_prop_grooved_reciprocal_yaw_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_grooved_relation_x_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[1], tdy[1];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_prop_grooved_relation_x_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_grooved_relation_x_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_prop_grooved_relation_x_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_grooved_relation_y_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[1], tdy[1];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_prop_grooved_relation_y_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_grooved_relation_y_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_prop_grooved_relation_y_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_hinge_freedom_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[8], tdx[8], ty[1], tdy[1];
  for (int k = 0; k < 8; ++k) { tx[k] = x[i*8+k]; tdx[k] = dx[i*8+k]; }
  hk_prop_hinge_freedom_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_hinge_freedom_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[8], thi[8], tsc[8], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 8; ++k) { tlo[k] = lo[i*8+k]; thi[k] = hi[i*8+k]; tsc[k] = sc[i*8+k]; }
  hk_prop_hinge_freedom_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_hinge_freedom_smul_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[9], tdx[9], ty[1], tdy[1];
  for (int k = 0; k < 9; ++k) { tx[k] = x[i*9+k]; tdx[k] = dx[i*9+k]; }
  hk_prop_hinge_freedom_smul_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_hinge_freedom_smul_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[9], thi[9], tsc[9], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 9; ++k) { tlo[k] = lo[i*9+k]; thi[k] = hi[i*9+k]; tsc[k] = sc[i*9+k]; }
  hk_prop_hinge_freedom_smul_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_hinge_reciprocal_swing_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_prop_hinge_reciprocal_swing_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_hinge_reciprocal_swing_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_prop_hinge_reciprocal_swing_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_m12_carries_dish_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_prop_m12_carries_dish_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_m12_carries_dish_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_prop_m12_carries_dish_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_mastClears_hashemi_iff_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_prop_mastClears_hashemi_iff_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_mastClears_hashemi_iff_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_prop_mastClears_hashemi_iff_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_mul_bounds_neg_pos_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[1], tdy[1];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_prop_mul_bounds_neg_pos_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_mul_bounds_neg_pos_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_prop_mul_bounds_neg_pos_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_mul_bounds_pos_pos_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[1], tdy[1];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_prop_mul_bounds_pos_pos_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_mul_bounds_pos_pos_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_prop_mul_bounds_pos_pos_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_play_budget_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_prop_play_budget_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_play_budget_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_prop_play_budget_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_plumbed_shift_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_prop_plumbed_shift_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_plumbed_shift_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_prop_plumbed_shift_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_postTop_on_rail_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[1], tdy[1];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_prop_postTop_on_rail_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_postTop_on_rail_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_prop_postTop_on_rail_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_postTops_apart_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[1], tdy[1];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_prop_postTops_apart_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_postTops_apart_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_prop_postTops_apart_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_postTops_level_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[12], tdx[12], ty[1], tdy[1];
  for (int k = 0; k < 12; ++k) { tx[k] = x[i*12+k]; tdx[k] = dx[i*12+k]; }
  hk_prop_postTops_level_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_postTops_level_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[12], thi[12], tsc[12], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 12; ++k) { tlo[k] = lo[i*12+k]; thi[k] = hi[i*12+k]; tsc[k] = sc[i*12+k]; }
  hk_prop_postTops_level_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_postTops_offAxis_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[13], tdx[13], ty[1], tdy[1];
  for (int k = 0; k < 13; ++k) { tx[k] = x[i*13+k]; tdx[k] = dx[i*13+k]; }
  hk_prop_postTops_offAxis_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tx[10], tx[11], tx[12], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_postTops_offAxis_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[13], thi[13], tsc[13], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 13; ++k) { tlo[k] = lo[i*13+k]; thi[k] = hi[i*13+k]; tsc[k] = sc[i*13+k]; }
  hk_prop_postTops_offAxis_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_reachesVertical_iff_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_prop_reachesVertical_iff_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_reachesVertical_iff_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_prop_reachesVertical_iff_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_rim_under_F_iff_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_prop_rim_under_F_iff_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_rim_under_F_iff_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_prop_rim_under_F_iff_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_rollerRadius_pos_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[1], tdy[1];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_prop_rollerRadius_pos_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_rollerRadius_pos_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_prop_rollerRadius_pos_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_rollerRadius_sq_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[6], tdx[6], ty[1], tdy[1];
  for (int k = 0; k < 6; ++k) { tx[k] = x[i*6+k]; tdx[k] = dx[i*6+k]; }
  hk_prop_rollerRadius_sq_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_rollerRadius_sq_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[6], thi[6], tsc[6], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 6; ++k) { tlo[k] = lo[i*6+k]; thi[k] = hi[i*6+k]; tsc[k] = sc[i*6+k]; }
  hk_prop_rollerRadius_sq_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_rotz_dot_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[7], tdx[7], ty[1], tdy[1];
  for (int k = 0; k < 7; ++k) { tx[k] = x[i*7+k]; tdx[k] = dx[i*7+k]; }
  hk_prop_rotz_dot_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_rotz_dot_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[7], thi[7], tsc[7], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 7; ++k) { tlo[k] = lo[i*7+k]; thi[k] = hi[i*7+k]; tsc[k] = sc[i*7+k]; }
  hk_prop_rotz_dot_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_screwLength_eq_focal_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_prop_screwLength_eq_focal_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_screwLength_eq_focal_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_prop_screwLength_eq_focal_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_screwTwist_zero_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[2], tdx[2], ty[1], tdy[1];
  for (int k = 0; k < 2; ++k) { tx[k] = x[i*2+k]; tdx[k] = dx[i*2+k]; }
  hk_prop_screwTwist_zero_jvp(tx[0], tx[1], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_screwTwist_zero_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[2], thi[2], tsc[2], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 2; ++k) { tlo[k] = lo[i*2+k]; thi[k] = hi[i*2+k]; tsc[k] = sc[i*2+k]; }
  hk_prop_screwTwist_zero_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_screw_freedom_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[9], tdx[9], ty[1], tdy[1];
  for (int k = 0; k < 9; ++k) { tx[k] = x[i*9+k]; tdx[k] = dx[i*9+k]; }
  hk_prop_screw_freedom_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_screw_freedom_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[9], thi[9], tsc[9], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 9; ++k) { tlo[k] = lo[i*9+k]; thi[k] = hi[i*9+k]; tsc[k] = sc[i*9+k]; }
  hk_prop_screw_freedom_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_screw_reciprocal_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_prop_screw_reciprocal_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_screw_reciprocal_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_prop_screw_reciprocal_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_slackHarmless_of_budget_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_prop_slackHarmless_of_budget_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_slackHarmless_of_budget_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_prop_slackHarmless_of_budget_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_slackHarmless_of_lever_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_prop_slackHarmless_of_lever_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_slackHarmless_of_lever_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_prop_slackHarmless_of_lever_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_sq_bounds_neg_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_prop_sq_bounds_neg_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_sq_bounds_neg_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_prop_sq_bounds_neg_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_strut_resists_lean_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[10], tdx[10], ty[1], tdy[1];
  for (int k = 0; k < 10; ++k) { tx[k] = x[i*10+k]; tdx[k] = dx[i*10+k]; }
  hk_prop_strut_resists_lean_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tx[8], tx[9], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_strut_resists_lean_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[10], thi[10], tsc[10], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 10; ++k) { tlo[k] = lo[i*10+k]; thi[k] = hi[i*10+k]; tsc[k] = sc[i*10+k]; }
  hk_prop_strut_resists_lean_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_sunDir_rot_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_prop_sunDir_rot_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_sunDir_rot_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_prop_sunDir_rot_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_sunInDish_equivariant_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_prop_sunInDish_equivariant_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_sunInDish_equivariant_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_prop_sunInDish_equivariant_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_swingFocus_circle_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_prop_swingFocus_circle_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_swingFocus_circle_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_prop_swingFocus_circle_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_swingNormal_unit_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_prop_swingNormal_unit_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_swingNormal_unit_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_prop_swingNormal_unit_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_swing_focusCircle_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_prop_swing_focusCircle_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_swing_focusCircle_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_prop_swing_focusCircle_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_swing_lift_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_prop_swing_lift_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_swing_lift_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_prop_swing_lift_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_tension_le_of_holds_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_prop_tension_le_of_holds_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_tension_le_of_holds_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_prop_tension_le_of_holds_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_trackerBudget_iff_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_prop_trackerBudget_iff_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_trackerBudget_iff_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_prop_trackerBudget_iff_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_tracker_margin_hashemi_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[1], tdx[1], ty[1], tdy[1];
  for (int k = 0; k < 1; ++k) { tx[k] = x[i*1+k]; tdx[k] = dx[i*1+k]; }
  hk_prop_tracker_margin_hashemi_jvp(tx[0], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_tracker_margin_hashemi_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[1], thi[1], tsc[1], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 1; ++k) { tlo[k] = lo[i*1+k]; thi[k] = hi[i*1+k]; tsc[k] = sc[i*1+k]; }
  hk_prop_tracker_margin_hashemi_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_tracking_power_tiny_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_prop_tracking_power_tiny_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_tracking_power_tiny_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_prop_tracking_power_tiny_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_wireLever_edge_formula_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[5], tdx[5], ty[1], tdy[1];
  for (int k = 0; k < 5; ++k) { tx[k] = x[i*5+k]; tdx[k] = dx[i*5+k]; }
  hk_prop_wireLever_edge_formula_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_wireLever_edge_formula_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[5], thi[5], tsc[5], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 5; ++k) { tlo[k] = lo[i*5+k]; thi[k] = hi[i*5+k]; tsc[k] = sc[i*5+k]; }
  hk_prop_wireLever_edge_formula_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_wireLever_pos_iff_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_prop_wireLever_pos_iff_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_wireLever_pos_iff_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_prop_wireLever_pos_iff_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_wireLever_rest_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_prop_wireLever_rest_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_wireLever_rest_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_prop_wireLever_rest_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_wire_recip_swing_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[8], tdx[8], ty[1], tdy[1];
  for (int k = 0; k < 8; ++k) { tx[k] = x[i*8+k]; tdx[k] = dx[i*8+k]; }
  hk_prop_wire_recip_swing_jvp(tx[0], tx[1], tx[2], tx[3], tx[4], tx[5], tx[6], tx[7], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_wire_recip_swing_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[8], thi[8], tsc[8], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 8; ++k) { tlo[k] = lo[i*8+k]; thi[k] = hi[i*8+k]; tsc[k] = sc[i*8+k]; }
  hk_prop_wire_recip_swing_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_wire_taut_iff_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[4], tdx[4], ty[1], tdy[1];
  for (int k = 0; k < 4; ++k) { tx[k] = x[i*4+k]; tdx[k] = dx[i*4+k]; }
  hk_prop_wire_taut_iff_jvp(tx[0], tx[1], tx[2], tx[3], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_wire_taut_iff_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[4], thi[4], tsc[4], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 4; ++k) { tlo[k] = lo[i*4+k]; thi[k] = hi[i*4+k]; tsc[k] = sc[i*4+k]; }
  hk_prop_wire_taut_iff_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}
kernel void mk_prop_yaw_lifts_nothing_jvp(device const float* x [[buffer(0)]], device const float* dx [[buffer(1)]], device float* y [[buffer(2)]], device float* dy [[buffer(3)]], device const int* n [[buffer(4)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tx[3], tdx[3], ty[1], tdy[1];
  for (int k = 0; k < 3; ++k) { tx[k] = x[i*3+k]; tdx[k] = dx[i*3+k]; }
  hk_prop_yaw_lifts_nothing_jvp(tx[0], tx[1], tx[2], tdx, ty, tdy);
  for (int k = 0; k < 1; ++k) { y[i*1+k] = ty[k]; dy[i*1+k] = tdy[k]; }
}
kernel void mk_prop_yaw_lifts_nothing_box(device const float* lo [[buffer(0)]], device const float* hi [[buffer(1)]], device const float* sc [[buffer(2)]], device float* olo [[buffer(3)]], device float* ohi [[buffer(4)]], device float* oL [[buffer(5)]], device const int* n [[buffer(6)]], uint i [[thread_position_in_grid]]) {
  if ((int)i >= n[0]) return;
  float tlo[3], thi[3], tsc[3], rlo[1], rhi[1], rL[1];
  for (int k = 0; k < 3; ++k) { tlo[k] = lo[i*3+k]; thi[k] = hi[i*3+k]; tsc[k] = sc[i*3+k]; }
  hk_prop_yaw_lifts_nothing_box(tlo, thi, tsc, rlo, rhi, rL);
  for (int k = 0; k < 1; ++k) { olo[i*1+k] = rlo[k]; ohi[i*1+k] = rhi[k]; oL[i*1+k] = rL[k]; }
}